#!/bin/bash
# Environment variables are variables accessible to any program or script
# running in current shell session.
# They are created just like local variables, but using the keyword `export`
# intead.
export GLOBAL_VAR="I'm a global variable"

# $HOME: The current user's home directory
# $PATH: A colon-separated list of directories in which the shell looks for
# commands
# $PWD: The current working directory
# $RANDOM: The integer between 0 and 32767
# $UID: The numeric, real user ID of the current user.
# $PS1: The primary propt string.
# $PS2: The secondary prompt string.